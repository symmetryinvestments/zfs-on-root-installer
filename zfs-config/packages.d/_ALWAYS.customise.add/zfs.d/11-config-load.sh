#
# Load configuration from external files
#

# TODO
# - if the boot media has some config, import it
#   (should take advantage of the /conf.d processing in the initramfs)
# - if the target dir (/mnt) has some config, import that too
#   (allows assisting with the stage1/stage2 resume)
